<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/img_price_it"
        android:label="@string/app_name"
        android:roundIcon="@drawable/img_price_it"
        android:supportsRtl="true"
        android:theme="@style/Theme.Skeleton"
        android:name="project.priceit.app.HiltApplication"
        tools:targetApi="31">
        <activity
            android:name="project.priceit.feature.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Skeleton">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>